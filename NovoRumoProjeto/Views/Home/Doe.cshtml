@model NovoRumoProjeto.Models.UserViewModel

<div class="white-content" id="seja-um-doador">
    <div class="grid-container">
        <div class="grid-x">
            <div class="small-12 medium-12 large-6 cell text-center">
                <h2>Sócio amigo</h2>
            </div>
        </div>
        <div class="grid-x">
            <div class="small-2 medium-4 cell"></div>
            <div class="small-8 medium-4 cell text-center quote">
                <img src="images/idea-icon.png">
                <a data-toggle="docao-modal">
                    <h4>Doe agora</h4>
                </a>
            </div>
        </div>
        <div class="grid-x">
            <div class="small-8 medium-4 large-8 cell hide-for-small-only"></div>
            <div class="small-12 medium-12 large-4 cell text-center">
                <p>
                    Sua doação é de extrema importância para nós, pois com ela podemos manter nossas atividades, realizar
                    manutenção e para o desenvolvimento de novos projetos para nossa Associação.
                </p>
            </div>
        </div>
    </div>

    <div class="reveal large" id="docao-modal" data-reveal data-close-on-click="true" data-animation-in="fade-in" data-animation-out="fade-out">
        <div class="grid-x">
            <div class="small-12 medium-9 cell">
                <h4>Seja um doador</h4>
                @using (Html.BeginForm("Cadastrar", "User", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    @Html.LabelFor(model => model.Name)
                    @Html.TextBoxFor(model => model.Name)
                    @Html.ValidationMessageFor(model => model.Name)
                    <input type="submit" class="button primary" value="salvar"/>
                }
            </div>
        </div>
        <button class="close-button" data-close aria-label="Close reveal" type="button">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
</div>

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

